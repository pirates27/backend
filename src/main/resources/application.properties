# Spring Application Name
spring.application.name=landlens

# Database Connection (with environment variables and development defaults)
spring.datasource.url=${DB_URL:jdbc:mysql://srv1117.hstgr.io:3306/u833088220_Priya_teamlead?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useInformationSchema=true}
spring.datasource.username=${DB_USERNAME:u833088220_Priya_teamlead}
spring.datasource.password=${DB_PASSWORD:Priya_teamlead@1234567}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.continue-on-error=true
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Spring Security Basic Disable
spring.security.basic.enabled=false

# JWT Properties (with environment variables and development defaults)
landlens.jwt.secret=${JWT_SECRET:9a2f3f4e5d6c7b8a9f0e1d2c3b4a5f6e7d8c9b0a1f2e3d4c5b6a7f8e9d0c1b2a3}
landlens.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
landlens.jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:2592000000}

# Hikari Connection Pool Settings for local dev
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=58000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.keepalive-time=15000
spring.datasource.hikari.validation-timeout=3000

# Actuator Endpoints Config (Health Check & Metrics)
management.endpoints.web.exposure.include=health,metrics
management.endpoint.health.show-details=always

openai.api.key=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